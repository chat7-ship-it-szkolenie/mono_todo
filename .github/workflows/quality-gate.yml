name: Quality Gate

on:
  push:
    paths:
      - "services/backend/**"
      - ".github/workflows/quality-gate.yml"
  pull_request:
    paths:
      - "services/backend/**"
      - ".github/workflows/quality-gate.yml"

defaults:
  run:
    working-directory: services/backend

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: services/backend/requirements.txt

      - name: Install dev tools
        run: pip install ruff black

      - name: ruff check
        run: ruff check .

      - name: black check
        run: black --check .

  test:
    name: Coverage & Mutation
    runs-on: ubuntu-latest
    env:
      JWT_SECRET_KEY: ci-test-secret-not-for-production
      DATABASE_URL: sqlite:///./test_todo.db
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: services/backend/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt pytest-cov mutmut

      - name: Run migrations
        run: alembic upgrade head

      - name: Run tests with coverage
        run: pytest -v --tb=short --cov=app --cov-report=term-missing --cov-fail-under=80

      - name: Mutation testing
        run: mutmut run --paths-to-mutate app

      - name: Clean up test DB
        if: always()
        run: rm -f test_todo.db
